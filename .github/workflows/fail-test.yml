name: Fail Test

on:
  workflow_dispatch:   # 버튼 눌러서 수동 실행 가능
  push:                # main 브랜치에 push되면 실행
    branches: [ "main" ]

jobs:
  fail-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run failing step
        run: |
          echo "This step will fail"
          exit 1
